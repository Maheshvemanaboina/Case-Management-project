trigger AccountTeamMemberTrigger on AccountTeamMember (before update,after update,before insert,after insert) {
    //AccountTeamMemberTriggerHandler atm = new AccountTeamMemberTriggerHandler();
    //atm.process();
}